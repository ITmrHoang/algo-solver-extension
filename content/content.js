/* ── Content script: minimal — just listens for clipboard commands ── */
// This content script is injected to enable clipboard operations
// triggered by the service worker via chrome.scripting.executeScript.
// It doesn't need to do anything on its own.
